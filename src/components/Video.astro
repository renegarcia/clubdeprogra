---
const {src, width=320, height=240, loop=false } = Astro.props;
---



<video width={width} height={height} controls loop={loop ? 'loop' : undefined}>
    <source src={src} type="video/mp4">
    Your browser does not support the video tag.
</video>
